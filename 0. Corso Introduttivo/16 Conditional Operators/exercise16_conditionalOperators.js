let isSunnyDay = false;
let isHomeworkCompleted = true;

isSunnyDay && isHomeworkCompleted ? console.log("Jesse can goes out to play"): console.log("Jesse stays home");

// Per costrutti ternari molto articolati
isSunnyDay && isHomeworkCompleted 
    ? console.log("Jesse can goes out to play")
    : console.log("Jesse stays home");